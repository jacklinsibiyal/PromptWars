import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
});

export const metadata: Metadata = {
  title: "TeamFlow — Modern Project Management",
  description:
    "A beautiful, intuitive project management tool for developers and product managers. Track issues, manage sprints, and collaborate in real-time.",
  keywords: ["project management", "team collaboration", "kanban", "agile", "sprint planning"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
